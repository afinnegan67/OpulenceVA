import { Transport } from '@modelcontextprotocol/sdk/shared/transport.js';
import { IAirtableService, IAirtableMCPServer } from './types.js';
export declare class AirtableMCPServer implements IAirtableMCPServer {
    private server;
    private airtableService;
    private readonly SCHEMA_PATH;
    constructor(airtableService: IAirtableService);
    private initializeHandlers;
    private handleListResources;
    private handleReadResource;
    private handleListTools;
    private handleCallTool;
    connect(transport: Transport): Promise<void>;
    close(): Promise<void>;
}
